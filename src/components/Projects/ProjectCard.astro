---
import { Image } from "astro:assets";

export interface Props {
  projectId: string;
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
  tags: string[];
}

const { projectId, title, description, imageUrl, imageAlt, tags } = Astro.props;
---

<article
  class="relative flex flex-col overflow-hidden rounded-2xl shadow-lg group"
>
  
</article>